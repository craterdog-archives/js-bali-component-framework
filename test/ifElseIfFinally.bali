{
    if false then {
        $component := (3, 4i)
    } else if component < 5 then {
        $component := 1.2
    } else {
        $component := - -pi i
    } catch $exception matching template with {
        publish exception
        throw exception
    } finish with {
        $done := true
    }
}