[
    $components: [
        3E23("m/s")
        pi($units: "radians")
        [1, 2, 3](
            $type: $Stack($Number([1..infinity]))
        )
    ]
    $variables: [
        x
        foo
        item5
        index
        example
        verylongvariablename
    ]
    $functions: [
        random(count)
        random(count)[1]
        random($count: 16)
        random($count: 16).sort(increasing)
        random(16).sort()[-x]
    ]
    $groupings: [
        (x + y) * z
        ((x + y) * z) - (a or (b and c))
        ([1, 2] + [2, 3])[1]
        ([1, 2] + [2, 3]).reverse()
        ([1, 2] + [2, 3]).reverse(foo, bar)[index]
    ]
    $dereferences: [
        @result
        @foo.bar()
        @(foo[x])
    ]
    $messages: [
        foo.bar()
        foo.bar(baz)
        foo.bar(5).random()
        foo.bar()[-index]
        [1, 2, 3].reverse()
        products[$yoyo, "Duncan"].calculateUPC()
    ]
    $indices: [
        values[$symbol]
        array[-1]
        [1, 2, 3][2]
        [[$a, $b], 2, 3][1, 2]
    ]
    $factorials: [
        5!
        big!
        foo()!
        base[index]!
        [1, 2, 3].reverse()!
    ]
    $exponentials: [
        2 ^ 8
        e ^ power
        x ^ y
        random() ^ exponent
        base ^ power(x)
    ]
    $inversions: [
        -x
        - -pi
        /exponential
        *flip
        / *(3, 4i)
    ]
    $geometrics: [
        -x * scaler
        6 / dividend
        -7 // modulo
    ]
    $arithmetics: [
        7 + 2
        5 + addend
        z - subtractend
    ]
    $magnitudes: [
        |-3|
        |-y|
        |foo()|
        |foo.bar(8)|
    ]
    $comparisons: [
        scaler < 8
        x = y
        big > 2
        value is [$foo]
        component matches any
    ]
    $complements: [
        not true
        not c
        not list.isEmpty()
    ]
    $logicals: [
        set and [1, 2, 3]
        set sans [$a, $b, $c]
        a or foo.bar()
        ["one", "two"] xor ["one"]
    ]
    $defaults: [
        optional ? true
        optional ? default
    ]
]